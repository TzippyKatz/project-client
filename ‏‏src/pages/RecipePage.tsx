import { useLocation } from 'react-router-dom';
// import MealRecipe from '../components/recipe/recipySearch'

export const RecipePage = () => {
//   const location = useLocation();

//   const mealId = location.state?.mealId; // ה-mealId שנשלח ב-state

//   if (!mealId) {
//     return <div>מזהה הארוחה לא נמצא</div>;
//   }

  return (
    // <MealRecipe
    //   mealIdProps={mealId}  // כאן שולחים את ה-mealId לדף המתכון
    //   isOpen={true}          // אפשר לשנות את זה אם יש צורך
    //   onClose={() => {}}     // אתה יכול לעדכן את הפונקציה לפי הצורך
    // />
    <div></div>
  );
};
